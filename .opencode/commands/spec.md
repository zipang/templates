---
description: Define requirements & acceptance criteria before coding
---
Use the `spec-driven-development` and `interview-me` skills to create a plan for the user intent: 
> $ARGUMENTS

Allocate the next Ticket ID (`T0001`–`T9999`) by scanning `roadmap/` for existing `T\d{4}` directories, or reuse the Ticket ID if `$ARGUMENTS` provides one. Save the spec to `roadmap/TXXXX/spec.md`.
When the spec is generated, ask the user to review it: 
> Please review the spec at []<link>. 
> 1. If it seems OK we can proceed now to the tasks description and planification? 
> 2. Needs more refinements (type your answer)?

On approbation, procede to write the implementation plan using the `planning-and-task-breakdown` skill and the ticket context.

