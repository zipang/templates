{
	"name": "@temples/docs",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"description": "Documentation site for the temples packages, built with temples itself.",
	"scripts": {
		"build": "bun run src/build.ts",
		"serve": "bun run src/serve.ts"
	},
	"dependencies": {
		"@temples/engine": "workspace:*",
		"@temples/ssr": "workspace:*"
	}
}
